## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## How I tested it

<!-- `npm run build` passed? What you clicked in `npm run dev`. If you changed apps-script/Code.gs, did you redeploy a new version and test against it? -->

## Screenshots (if the UI changed)

## Checklist

- [ ] Branch is named `feature/…`, `fix/…`, or `chore/…`
- [ ] No secrets or `.env.local` in this PR
- [ ] README / `.env.example` / `vite-env.d.ts` updated if config changed
- [ ] If `Code.gs` changed: header comment updated and a new Apps Script version deployed
